"""Test package for bierre.

Test directories are real packages (`__init__.py` everywhere) so that shared
helpers can be imported as `tests.factories` / `tests.support.*` and so two
test modules in different layers may share a filename.
"""
