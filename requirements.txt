# Core runtime — the pipeline runs with just these two.
requests>=2.31.0
PyYAML>=6.0.0

# Web UI adapter (webapp/server.py).
fastapi>=0.116.0
uvicorn>=0.34.0

# Optional but recommended: better relevance ranking.
scikit-learn>=1.3.0
rank-bm25>=0.2.2
rapidfuzz>=3.0.0
