# Test/dev dependencies. Install with:
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0
pytest-cov>=5.0.0
# Required by fastapi.testclient.TestClient (Starlette's ASGI transport).
httpx>=0.27.0
# Validates GET /api/capabilities against webapp/schemas/capabilities.json.
jsonschema>=4.21.0
